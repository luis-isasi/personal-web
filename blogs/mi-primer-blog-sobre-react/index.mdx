---
title: "Este es mi primer blog sobre react"
contentPreview: 'Esta es la descripcion para mi primer blog, espero que todo vaya bien'
createdAt: "14/07/2021"
categories: "react"
---

### Introducción
Este blog, es un blog de test, hablaremos sobre [react](https://reactjs.org/).

### ¿Cómo crear un proyecto de react con create-react-app?
El siguiente comando, creará todo el septup de inicio para un proyecto en react. recuerda que tambien puedes crear esos septups por tu cuenta.

```
  npx create-react-app my-app
```

### ¿Cómo crear un componente en react?
Creamos un archivo con el nombre de tu componente(recuerda que los nombres de los componentes deben iniciar en mayúscula),
dentro del archivo escribimos el siguiente código.

```
const MyComponent = () => {
  const text = "Hello World!";

  return (
    <div>
      <span>My first component</span>
      <span>{text}</span>
    </div>
  );
};

export default MyComponent;
```